@tailwind base;
@tailwind components;
@tailwind utilities;

.poppins {
  font-family: 'Poppins', sans-serif;
}

.text-field {
  background-color:  #484B51;
  @apply p-3 focus:ring-2 focus:ring-green-300 text-white rounded-xl;
}

.btn {
  @apply poppins w-full p-3 rounded-xl border-none;
}
